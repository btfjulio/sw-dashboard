//get button from requested link
var bitlyButton = document.getElementById(`<%= @suplemento[:store_code] %>`)
bitlyButton.innerHTML = `<input id="C<%= @suplemento[:store_code] %>" value="<%= @shorten_url %>">
                         <button class="clipboard-btn" data-clipboard-target="#C<%= @suplemento[:store_code] %>">
                            Copy
                         </button>`
var btns = document.querySelectorAll('.clipboard-btn');
btns.forEach(function(btn){
    new Clipboard(btn); 
});
//get new value to copy
